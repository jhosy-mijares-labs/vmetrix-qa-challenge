{
  "name": "vmetrix-qa-challenge",
  "version": "1.0.0",
  "description": "QA Automation Challenge | Playwright + TypeScript + Allure | UI & API Testing",
  "main": "index.js",
  "scripts": {
    "clean:results": "node -e \"require('fs').rmSync('allure-results', { recursive: true, force: true })\"",
    "test:ui": "npm run clean:results && npx playwright test --project='UI'",
    "test:ui:standard": "npm run clean:results && npx playwright test --project='UI [standard]'",
    "test:ui:problem": "npm run clean:results && npx playwright test --project='UI [problem]'",
    "test:ui:performance": "npm run clean:results && npx playwright test --project='UI [performance_glitch]'",
    "test:api": "npm run clean:results && npx playwright test --project='API Tests'",
    "test:all": "npm run clean:results && npx playwright test",
    "report:generate": "allure generate allure-results --clean -o allure-report",
    "report:open": "allure open allure-report",
    "report": "npm run report:generate && npm run report:open",
    "test:report": "npm run test:all; npm run report"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jhosy-mijares-labs/vmetrix-qa-challenge.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/jhosy-mijares-labs/vmetrix-qa-challenge/issues"
  },
  "homepage": "https://github.com/jhosy-mijares-labs/vmetrix-qa-challenge#readme",
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "allure-js-commons": "^3.10.0",
    "allure-playwright": "^3.10.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}